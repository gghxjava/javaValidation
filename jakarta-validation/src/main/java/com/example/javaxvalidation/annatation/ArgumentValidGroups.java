package com.example.javaxvalidation.annatation;

public class ArgumentValidGroups {

    // 定义编辑操作的验证分组接口
    public interface editGroup {
        // 这里不需要定义任何方法，接口仅作为验证分组的标记
    }

    // 如果需要，您可以在这里继续定义更多的分组接口
    // 例如定义一个用于创建操作的验证分组接口
    public interface createGroup {
        // 同样，这里不需要定义任何方法
    }

}
